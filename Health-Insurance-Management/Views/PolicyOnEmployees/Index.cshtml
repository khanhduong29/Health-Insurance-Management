@model IEnumerable<Health_Insurance_Management.Models.PolicyOnEmployee>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Admin/Layout/_Layout.cshtml";
}

<section class="content-header">
    <h1>
        Health-Insurance-Management
    </h1>
    <ol class="breadcrumb">
        <li><a href="#"><i class="fa fa-dashboard"></i> Home</a></li>
        <li><a href="#">Policy</a></li>
        <li class="active">Employee Policy</li>
    </ol>
</section>
<!-- Main content -->
<section class="content">
    <button class="btn btn-success" style="color: white;">
        @Html.ActionLink("Create New", "Create")
    </button>
    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                <div class="box-header">
                    <h3 class="box-title">Policies</h3>
                </div>
                <!-- /.box-header -->
                <div class="box-body">
                    <table id="example1" class="table table-striped table-hover">
                        <thead>
                            <tr>
                                <th>Id</th>
                                <th>@Html.DisplayNameFor(model => model.Employee.EmployeeName)</th>
                                <th>@Html.DisplayNameFor(model => model.Policy.PolicyName)</th>
                                <th>@Html.DisplayNameFor(model => model.StartDate)</th>
                                <th>@Html.DisplayNameFor(model => model.EndDate)</th>
                                <th>@Html.DisplayNameFor(model => model.Status)</th>
                                <th>@Html.DisplayNameFor(model => model.CreatedDate)</th>
                                <th>@Html.DisplayNameFor(model => model.UpdatedDate)</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Id)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Employee.EmployeeName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Policy.PolicyName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.StartDate)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.EndDate)
                                    </td>
                                    <td>
                                        @if (item.Status == Health_Insurance_Management.Models.Enum.Status.Active)
                                        {
                                            <span class="label label-success">@Html.DisplayFor(modelItem => item.Status)</span>
                                        }
                                        else
                                        {
                                            <span class="label label-warning">@Html.DisplayFor(modelItem => item.Status)</span>
                                        }

                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.CreatedDate)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.UpdatedDate)
                                    </td>
                                    <td>
                                        <div class="btn-group">
                                            <button type="button" class="btn btn-warning dropdown-toggle" data-toggle="dropdown">Action</button>
                                            <ul class="dropdown-menu" role="menu">
                                                @if (item.Status == Health_Insurance_Management.Models.Enum.Status.Active)
                                                {
                                                    <li><a href="@Url.Action("ChangeStatus", "PolicyOnEmployees", new { id = item.Id })">DeActive</a></li>
                                                }
                                                else
                                                {
                                                    <li><a href="@Url.Action("ChangeStatus", "PolicyOnEmployees", new { id = item.Id })">Active</a></li>
                                                }
                                                <li class="divider"></li>
                                                <li><a href="@Url.Action("Details", "PolicyOnEmployees", new { id = item.Id })">View Detail</a></li>
                                                <li><a href="@Url.Action("Edit", "PolicyOnEmployees", new { id = item.Id })">Edit</a></li>
                                                <li><a href="@Url.Action("Delete", "PolicyOnEmployees", new { id = item.Id })">Delete</a></li>
                                            </ul>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
                <!-- /.box-body -->
            </div>
            <!-- /.box -->
        </div>
        <!-- /.col -->
    </div>
    <!-- /.row -->
</section>
